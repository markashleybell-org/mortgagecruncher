@model IndexViewModel

@{
    var cols = new { Label = 3, Input = 3, Validation = 6 };
}
<div class="panel panel-variables" id="form-variables">
    <div class="panel-heading">Please enter some details about your mortgage</div>
    <div class="panel-body">
        <form asp-action="Calculate" asp-controller="Home" method="get" class="form-horizontal form-variables">
            <div class="form-group">
                <label asp-for="LoanValue" class="col-md-@(cols.Label) control-label"></label>
                <div class="col-md-@(cols.Input)">
                    <div class="input-group">
                        <span class="input-group-addon">£</span>
                        <input asp-for="LoanValue" class="form-control" data-val-required="The loan value is required" data-val-number="The loan value must be a number" />
                    </div>
                </div>
                <div class="col-md-@(cols.Validation) form-info">
                    <p class="form-control-static"><span asp-validation-for="LoanValue"></span></p>
                </div>
            </div>
            <div class="form-group">
                <label asp-for="TermYears" class="col-md-@(cols.Label) control-label"></label>
                <div class="col-md-@(cols.Input)">
                    <div class="input-group">
                        <input asp-for="TermYears" class="form-control" data-val-required="The mortgage term is required" data-val-number="The mortgage term must be a number" />
                        <span class="input-group-addon">years</span>
                    </div>
                </div>
                <div class="col-md-@(cols.Validation) form-info">
                    <p class="form-control-static"><span asp-validation-for="TermYears"></span></p>
                </div>
            </div>
            <div class="form-group">
                <label asp-for="StartDate" class="col-md-@(cols.Label) control-label"></label>
                <div class="col-md-@(cols.Input)">
                    <input asp-for="StartDate" class="form-control datepicker" data-val-required="The mortgage start date (DD/MM/YYYY) is required" />
                </div>
                <div class="col-md-@(cols.Validation) form-info">
                    <p class="form-control-static"><span asp-validation-for="StartDate"></span></p>
                </div>
            </div>
            <div class="form-group">
                <label asp-for="TermRate" class="col-md-@(cols.Label) control-label"></label>
                <div class="col-md-@(cols.Input)">
                    <div class="input-group">
                        <input asp-for="TermRate" class="form-control" data-val-required="The standard interest rate is required" data-val-number="The standard interest rate must be a number" />
                        <span class="input-group-addon">%</span>
                    </div>
                </div>
                <div class="col-md-@(cols.Validation) form-info">
                    <p class="form-control-static"><span asp-validation-for="TermRate"></span></p>
                </div>
            </div>
            <div class="form-group">
                <label asp-for="FixedTermYears" class="col-md-@(cols.Label) control-label"></label>
                <div class="col-md-@(cols.Input)">
                    <select asp-for="FixedTermYears" asp-items="Model.FixedRatePeriods" class="form-control"></select>
                </div>
                <div class="col-md-@(cols.Validation) form-info">
                    <p class="form-control-static"><span asp-validation-for="FixedTermYears"></span></p>
                </div>
            </div>
            <div class="form-group" id="fixed-term-group">
                <label asp-for="FixedTermRate" class="col-md-@(cols.Label) control-label"></label>
                <div class="col-md-@(cols.Input)">
                    <div class="input-group">
                        <input asp-for="FixedTermRate" class="form-control" data-val-required="The fixed interest rate is required" data-val-number="The fixed interest rate must be a number" />
                        <span class="input-group-addon">%</span>
                    </div>
                </div>
                <div class="col-md-@(cols.Validation) form-info">
                    <p class="form-control-static"><span asp-validation-for="FixedTermRate"></span></p>
                </div>
            </div>
            <script>
                document.getElementById('fixed-term-group').style.display = document.getElementById('FixedTermYears').value !== '' ? 'block' : 'none';
            </script>
            <div class="form-group">
                <label asp-for="OverpaymentInterval" class="col-md-@(cols.Label) control-label"></label>
                <div class="col-md-@(cols.Input)">
                    <select asp-for="OverpaymentInterval" asp-items="Model.OverpaymentIntervals" class="form-control"></select>
                </div>
                <div class="col-md-@(cols.Validation) form-info">
                    <p class="form-control-static"><span asp-validation-for="OverpaymentInterval"></span></p>
                </div>
            </div>
            <div id="overpayment-group">
                <div class="form-group">
                    <label asp-for="OverpaymentStartDate" class="col-md-@(cols.Label) control-label"></label>
                    <div class="col-md-@(cols.Input)">
                        <input asp-for="OverpaymentStartDate" class="form-control datepicker" data-val-required="The overpayment start date (DD/MM/YYYY) is required" />
                    </div>
                    <div class="col-md-@(cols.Validation) form-info">
                        <p class="form-control-static"><span asp-validation-for="OverpaymentStartDate"></span></p>
                    </div>
                </div>
                <div class="form-group">
                    <label asp-for="OverpaymentAmount" class="col-md-@(cols.Label) control-label"></label>
                    <div class="col-md-@(cols.Input)">
                        <div class="input-group">
                            <span class="input-group-addon">£</span>
                            <input asp-for="OverpaymentAmount" class="form-control" data-val-number="The overpayment amount must be a number" />
                        </div>
                    </div>
                    <div class="col-md-@(cols.Validation) form-info">
                        <p class="form-control-static"><span asp-validation-for="OverpaymentAmount"></span></p>
                    </div>
                </div>
                <div class="form-group">
                    <label asp-for="AmortisationScheduleType" class="col-md-@(cols.Label) control-label"></label>
                    <div class="col-md-@(cols.Input)">
                        <select asp-for="AmortisationScheduleType" asp-items="Html.GetEnumSelectList(typeof(AmortisationScheduleTypeViewModel))" class="form-control"></select>
                    </div>
                    <div class="col-md-@(cols.Validation) form-info">
                        <p class="form-control-static"><span asp-validation-for="AmortisationScheduleType"></span></p>
                    </div>
                </div>
            </div>
            <script>
                document.getElementById('overpayment-group').style.display = document.getElementById('OverpaymentInterval').value !== '' ? 'block' : 'none';
            </script>
            <div class="form-group">
                <div class="col-md-@(cols.Input + 1) col-lg-@(cols.Input) col-md-offset-@(cols.Label)">
                    <input type="submit" class="btn btn-primary btn-lg btn-block btn-main" value="Calculate Payment Schedule" />
                </div>
            </div>
            <input type="hidden" asp-for="Valid" />
            <input type="hidden" id="refresh" value="no" />
        </form>
    </div>
</div>
@if(Model.Valid) {
<script>
    document.getElementById('form-variables').style.display = 'none';
</script>
<p id="mortgage-summary">
    <strong>@Model.LoanValue.ToString("C")</strong> repayable over <strong>@Model.TermYears</strong> years at
    @if (Model.FixedTermYears.HasValue)
    {
        <strong>@Model.FixedTermRate% APR fixed for @Model.FixedTermYears</strong> @:years, then reverting to 
    }
    <strong>@Model.TermRate% SVR</strong>. 
    @if(Model.OverpaymentInterval.HasValue && Model.OverpaymentAmount.HasValue && Model.OverpaymentInterval > 0 && Model.OverpaymentAmount > 0) {
    @:An overpayment of <strong>@Model.OverpaymentAmount.Value.ToString("C")</strong> will be made <strong>every @Model.OverpaymentInterval months@((!string.IsNullOrWhiteSpace(Model.OverpaymentStartDate)) ? ", starting " + Model.OverpaymentStartDate : "").</strong> 
    }
    Loan paid off in <strong>@Model.ScheduleEntries.Last().PaymentDate.ToString("MMMM yyyy").</strong>
    <a href="#" id="show-form">[Change]</a>
</p>

}

@if(Model.ScheduleEntries.Count() > 0) {
<div class="panel panel-default table-responsive">
    <table class="table table-striped table-condensed">
        <thead>
            <tr>
                <th>Year</th>
                <th>Date</th>
                <th>Pmt. No.</th>
                <th>Interest</th>
                <th class="text-right">% APR</th>
                <th class="text-right">Payment</th>
                <th class="text-right">Interest</th>
                <th class="text-right">Principal</th>
                <th class="text-right">Balance</th>
            </tr>
        </thead>
        <tbody>
            @{
                var now = DateTime.Now;
                var currentYear = 0;
            }
            @foreach (var entry in Model.ScheduleEntries)
            {
                <tr class="@(entry.InterestType == InterestType.Fixed ? "fixed" : "variable")@((entry.PaymentDate.Year == now.Year && entry.PaymentDate.Month == now.Month) ? " current" : "")">
                    @if(currentYear != entry.PaymentDate.Year)
                    {
                    <td class="year" rowspan="@(13 - entry.PaymentDate.Month)">@entry.PaymentDate.Year</td>
                        currentYear = entry.PaymentDate.Year;
                    }
                    <td>@entry.PaymentDate.ToString("dd MMM")</td>
                    <td>@entry.PaymentNumber</td>
                    <td>@entry.InterestType</td>
                    <td class="text-right">@entry.InterestRate</td>
                    <td class="text-right">@entry.Payment.ToString("F2")</td>
                    <td class="text-right">@entry.Interest.ToString("F2")</td>
                    <td class="text-right">@entry.Principal.ToString("F2")</td>
                    <td class="text-right">@entry.Balance.ToString("F2")</td>
                </tr>
            }
        </tbody>
    </table>
</div>
}